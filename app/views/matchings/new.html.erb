<!-- <h1>Matchings#new</h1> -->

<h1>New Matching</h1>

<%= form_with model: @matching do |form| %>
  <div>
    <%= form.label :student_id %>
    <%= form.collection_select :student_id, @students, :id, :name %><br><br>
  </div>

  <div>
    <%= form.label :tutor_id %>
    <%= form.collection_select :tutor_id, @tutors, :id, :name %><br><br>
  </div>

  <div>
    <%= form.submit "Confirm Matching" %>
  </div>
<% end %>